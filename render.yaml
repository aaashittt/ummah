services:
  - type: web
    name: my-chatgpt-backend
    runtime: node
    buildCommand: npm install  # ✅ No "cd backend" needed
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 3000
      # DO NOT include OPENROUTER_API_KEY here!